import React from 'react';

const Nav = () => {
    return (
      <div>
        <header>Meet Me in the Middle</header>
        <button>Log in with Google</button>
      </div>
    )
};

export default Nav;
