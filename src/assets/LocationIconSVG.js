export const MarkerIcon = ({ iconColor }) => {
  return (
    <span className='material-symbols-outlined' style={{ color: iconColor }}>
      my_location
    </span>
  );
};

export const HouseIcon = ({ iconColor }) => {
  return (
    <span className='material-symbols-outlined' style={{ color: iconColor }}>
      home
    </span>
  );
};
